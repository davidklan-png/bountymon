# Vault Maintenance Corrections Log
**Date:** 2026-05-05

---

## Issue #1: Bountymon Misplaced Files

### Files/Directories Affected:
- `/home/teabagger/.openclaw/vault/bountymon/` (directory)
- `/home/teabagger/.openclaw/vault/src/content/articles/` (4 article files)

### Problem:
Bountymon-specific files are stored in the Kinokomon vault instead of the Bountymon workspace.

### Root Cause:
The "Bountymon Daily Article" cron job (id: e1eae945-69c5-4a62-b34e-eb6ac8b18867) writes articles to the Kinokomon vault using the `cat` shell command with hardcoded paths in `/home/teabagger/.openclaw/vault/src/content/articles/`.

### Corrective Action Required:
1. Move `/home/teabagger/.openclaw/vault/bountymon/rss-feeds.json` to Bountymon workspace (or remove since unused)
2. Move article files to Bountymon workspace: `~/dev/projects/bountymon/src/content/articles/`
3. Update cron job to write to Bountymon workspace instead of Kinokomon vault
4. Remove empty `/home/teabagger/.openclaw/vault/src/` directory after migration

### Status:
- **NOT AUTO-FIXED** - Requires manual review to ensure Bountymon workspace is properly set up

---

## Issue #2: Duplicate Bio_HP Repository

### Files/Directories Affected:
- `/home/teabagger/.openclaw/vault/workspace/Bio_HP/` (entire git clone)

### Problem:
Duplicate clone of Bio_HP repository. Primary clone exists at `~/dev/projects/Bio_HP/`.

### Root Cause:
Manual cloning or incorrect workspace setup. The vault is not intended to hold git clones.

### Corrective Action Required:
1. Remove `/home/teabagger/.openclaw/vault/workspace/Bio_HP/` directory
2. Verify all work references use `~/dev/projects/Bio_HP/`

### Status:
- **NOT AUTO-FIXED** - Requires verification that the copy is truly redundant

---

## Issue #3: Transient Files Tracked in Git

### Files Affected:
- `hn_rss.xml`
- `hnrss_feed.xml`
- `techcrunch_feed.xml`
- `techcrunch_rss.xml`
- `venturebeat_feed.xml`
- `venturebeat_rss.xml`

### Problem:
RSS feed files are tracked in git but are transient data that gets updated regularly by the Bountymon cron job.

### Root Cause:
Missing `.gitignore` file in vault root.

### Corrective Action Required:
1. Create `.gitignore` in vault root
2. Add patterns: `*.xml`, `workspace/`, `src/`, `bountymon/`
3. Remove XML files from git tracking (but keep locally)

### Status:
- **NOT AUTO-FIXED** - Requires manual git operations

---

## Issue #4: Test/Temporary Files

### Files Affected:
- `/home/teabagger/.openclaw/vault/workspace/moltbook-credentials.json`
- `/home/teabagger/.openclaw/vault/workspace/testhtlm.html`
- `/home/teabagger/.openclaw/vault/state/` (empty directory)

### Problem:
Test files and empty directories cluttering vault workspace.

### Root Cause:
Development artifacts not cleaned up.

### Corrective Action Required:
1. Review and remove test files if no longer needed
2. Remove empty `state/` directory
3. Clean up `workspace/` directory if no longer needed

### Status:
- **NOT AUTO-FIXED** - Requires manual verification

---

## Issue #5: Hooks Directory

### Files Affected:
- `/home/teabagger/.openclaw/vault/hooks/kinokomon-debug-snapshot/`

### Problem:
Debug hooks directory in vault root - unclear if intentional.

### Root Cause:
Unknown - need to check if used by any OpenClaw functionality.

### Corrective Action Required:
1. Review OpenClaw documentation to confirm hooks directory placement
2. If not needed, archive or remove

### Status:
- **NOT AUTO-FIXED** - Requires investigation

---

## Summary

**Total Issues Found:** 5
**Auto-Fixed:** 0
**Requires Manual Action:** 5

### Priority Recommendations:
1. **HIGH:** Fix Issue #1 (Bountymon misplaced files) - cron job is actively writing to wrong location
2. **MEDIUM:** Fix Issue #2 (duplicate Bio_HP) - storage waste, confusion
3. **LOW:** Fix Issue #3 (gitignore) - improves repo cleanliness
4. **LOW:** Fix Issue #4 (test files) - cleanup
5. **LOW:** Fix Issue #5 (hooks) - investigate and clean up

---

*Log generated by Kinokomon vault maintenance cron job*
